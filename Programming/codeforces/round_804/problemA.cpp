#include <bits/stdc++.h>
using namespace std;

int main()
{
  long long int t, n, temp;
  cin >> t;
  while (t)
  {
    cin >> n;
    temp = 0;
    for (long long int i = 0; i <= n / 2 + 1; i++)
    {
      temp +=
    }

    t--
  }

  return 0;
}